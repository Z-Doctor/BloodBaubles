package zdoctor.bloodbaubles.api;

public interface IConsumeBloodOrb {
	// Items that extend this will consume Blood Orbs if they are craft with
	// them
	// Safe for any other items that have containers
}
